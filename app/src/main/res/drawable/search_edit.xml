<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:radius="15dp" /> <!-- Điều chỉnh bán kính để làm cong -->
    <solid
        android:color="@color/white" /> <!-- Màu nền của hình cong -->
    <stroke
        android:width="0.5dp"
        android:color="#000000" /> <!-- Màu viền của hình cong -->
</shape>